\documentclass[a4paper,12pt]{ctexart} % 使用 ctexart 文档类，原生支持中文

% ==================== 宏包引入 ====================
\usepackage{geometry}       % 页面边距设置
\usepackage{graphicx}       % 插入图片
\usepackage{float}          % 图片位置强制固定
\usepackage{booktabs}       % 三线表
\usepackage{amsmath}        % 数学公式
\usepackage{listings}       % 代码块
\usepackage{xcolor}         % 颜色支持
\usepackage{hyperref}       % 目录跳转和超链接
\usepackage{caption}        % 图表标题格式
\usepackage{subcaption}
\usepackage{fancyhdr}       % 页眉页脚

% ==================== 页面设置 ====================
\geometry{left=2.5cm, right=2.5cm, top=2.5cm, bottom=2.5cm}

% ==================== 代码高亮设置 ====================
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,
    postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2,
    columns=flexible
}
\lstset{style=mystyle, language=Python}

% ==================== 超链接设置 ====================
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=blue,
    citecolor=blue
}

% ==================== 文档信息 ====================
\title{\textbf{自然语言处理实验报告}\\ \large 基于大语言模型的中文分词性能评估与优化}
\author{姓名：张宗桪 \\ 学号：2023K8009991013 \\ 专业：人工智能}
\date{\today}

\begin{document}

% --- 封面 ---
\maketitle
\thispagestyle{empty} 
\newpage 

% --- 目录 ---
\pagestyle{plain}
\tableofcontents
\newpage 

% --- 正文设置 ---
\pagestyle{fancy} 
\fancyhf{} 
\fancyhead[R]{NLP第三次作业报告}
\fancyfoot[C]{\thepage} 
\setlength{\parskip}{0.5em} 

% ================= 正文开始 =================

\section{实验概述}
本次实验旨在利用大语言模型（LLM）完成中文分词任务。实验选取了不同参数规模的模型，基于《人民日报》语料及网络爬取的多源语料进行测试，评估其分词性能（Precision, Recall, F1-score），并探究模型参数量对性能的影响，最后通过 In-Context Learning (Few-shot) 方法尝试提升模型性能。

\subsection{实验环境与模型}
\begin{itemize}
    \item \textbf{API平台}: Gitee AI Serverless API (https://ai.gitee.com/v1)
    \item \textbf{测试模型}: 
    \begin{itemize}
        \item Qwen3-32B (作为主要测试对象)
        \item Qwen3-8B (用于参数量对比分析)
        \item DeepSeek-V3 (用于横向模型对比)
    \end{itemize}
    \item \textbf{评估指标}: 采用标准的 Precision ($P$), Recall ($R$) 和 F1-measure ($F1$)。
\end{itemize}

% --- 任务一 ---
\section{任务一：标准语料上的分词性能测试}
\subsection{数据来源与预处理}
利用北京大学标注的《人民日报》1998年1月分词语料，随机抽取50个句子作为测试集。数据预处理包括去除原标注中的词性标记，仅保留分词空格作为 Ground Truth。

\subsection{实验结果}
表 \ref{tab:pku_result} 展示了三个模型在标准新闻语料上的表现。

\begin{table}[H]
    \centering
    \caption{各模型在《人民日报》测试集上的分词性能}
    \label{tab:pku_result}
    \begin{tabular}{l c c c}
        \toprule
        \textbf{Model} & \textbf{Precision (\%)} & \textbf{Recall (\%)} & \textbf{F1-Score (\%)} \\
        \midrule
        Qwen3-8B      & 00.00 & 00.00 & 00.00 \\
        Qwen3-32B     & 00.00 & 00.00 & 00.00 \\
        DeepSeek-V3   & 00.00 & 00.00 & 00.00 \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{结果分析}
（此处填写分析：例如 DeepSeek-V3 是否因为更强的推理能力表现更好？Qwen3-32B 是否比 8B 版本有显著提升？）

% --- 任务二 ---
\section{任务二：多领域文本泛化能力测试}
\subsection{多源数据构建}
为了测试模型的泛化能力，利用 Python 爬虫获取了以下三类文本：
\begin{enumerate}
    \item \textbf{新闻语料}：正式书面语，语法严谨。
    \item \textbf{网络聊天}：口语化严重，包含俚语和非标准语法。
    \item \textbf{百度百科}：说明文性质，包含较多专有名词。
\end{enumerate}

\subsection{跨域性能对比}
\begin{table}[H]
    \centering
    \caption{Qwen3-32B 在不同文本类型上的性能对比}
    \label{tab:cross_domain}
    \begin{tabular}{l c c c}
        \toprule
        \textbf{Text Type} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
        \midrule
        News (In-Domain)     & 00.00 & 00.00 & 00.00 \\
        Chat (OOD)           & 00.00 & 00.00 & 00.00 \\
        Encyclopedia (OOD)   & 00.00 & 00.00 & 00.00 \\
        \bottomrule
    \end{tabular}
\end{table}

% --- 任务三 ---
\section{任务三：模型参数量对性能的影响分析}
本节对比 Qwen3 系列中 8B 和 32B 两个版本的表现。

\begin{figure}[H]
    \centering
    % 如果有生成的图片，取消下面注释并替换文件名
    % \includegraphics[width=0.8\textwidth]{images/scaling_analysis.png}
    \caption{参数量对 F1 值的影响 (示意图)}
    \label{fig:scaling}
\end{figure}

\noindent\textbf{分析结论}：
\begin{itemize}
    \item 随着参数量的增加，模型对长难句和歧义词的处理能力通常会有所提升。
    \item （此处填写你的具体发现：例如 32B 在未登录词（OOV）识别上是否优于 8B？）
\end{itemize}

% --- 任务四 ---
\section{任务四：分词性能提升实验}
\subsection{优化方法：Few-Shot Prompting}
针对大模型在 Zero-shot 场景下分词标准（如复合词切分）与北大标准不一致的问题，本实验提出了基于 \textbf{Few-shot (In-Context Learning)} 的优化方法。

构造的 Prompt 包含 4 个典型示例，涵盖了人名、日期和成语的处理规范。

\subsection{优化效果对比}
\begin{table}[H]
    \centering
    \caption{优化前后性能对比 (Qwen3-32B)}
    \label{tab:optimization}
    \begin{tabular}{l c c c}
        \toprule
        \textbf{Method} & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} \\
        \midrule
        Zero-shot (Baseline) & 00.00 & 00.00 & 00.00 \\
        \textbf{Few-shot (Ours)} & \textbf{00.00} & \textbf{00.00} & \textbf{00.00} \\
        \bottomrule
    \end{tabular}
\end{table}

\section{结论}
本实验系统评估了大语言模型在中文分词任务上的表现。实验表明，参数量更大的模型具备更好的语言理解能力，且通过 Few-shot 提示工程可以显著纠正模型的分词风格，使其更贴近特定的标注规范（如 PKU 标准）。

% --- 附录 ---
\newpage
\appendix
\section{关键代码附录}

\subsection{API 调用与 Few-shot 实现}
\begin{lstlisting}[language=Python]
# 核心代码片段
FEW_SHOT_PROMPT = """
Example 1:
Input: 迈向充满希望的新世纪
Output: 迈向 充满 希望 的 新 世纪
...
"""

def process_batch(lines_batch):
    # ... (此处粘贴你的 pred.py 或 few_shots.py 核心逻辑)
    pass
\end{lstlisting}

\end{document}